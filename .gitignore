node_modules/
dist/
coverage/
*.tsbuildinfo
.env
.env.*

# Benchmark run artifacts (regenerated by benchmarks/**/run-full.ts)
benchmarks/**/results-full.json
benchmarks/**/run-log.txt

# Real OpenClaw session transcripts — never publish (PII/canaries). Local only.
tests/fixtures/openclaw-sessions/

demo/goldens/*.eval.mjs

# Generated triage report artifacts (regenerate via demo/gen-report.mjs)
reports/
